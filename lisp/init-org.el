;; config for org mode
;;----------- config for org -------------
;;highligh for org mode
(require 'org)
(setq org-src-fontify-natively t)

;;config for org-agenda
(setq org-agenda-file '("~/org"))

(provide 'org)

;;------------end config for org--------
